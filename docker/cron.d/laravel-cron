# 日本時間の8:00と18:00に統計情報を更新（直接実行）
# タイムゾーンを明示的に設定し、ログディレクトリを確実に作成
0 8,18 * * * root cd /var/www/html && TZ=Asia/Tokyo php artisan stats:update >> /var/log/cron.log 2>&1 || echo "$(date): stats:update failed" >> /var/log/cron.log
